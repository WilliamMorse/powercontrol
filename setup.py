from setuptools import setup

setup(name='powercontrol',
      version='0.0.2',
      packages='powercontrol',
      license='MIT')
      #install_requires=['typing'])
                        #'serial'])
